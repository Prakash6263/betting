PAGE-BY-PAGE UI VERIFICATION  (Next.js localhost:3000  vs  Original HTML localhost:8090)
Compare: Google Translate blocked => both in original English markup. Screenshots saved in tools/shots.

MATCH   /home   [extra: language dropdown added + nav = SPA routes (intended)]
MATCH   /login   [extra: language dropdown added + nav = SPA routes (intended)]
MATCH   /register   [extra: language dropdown added + nav = SPA routes (intended)]
MATCH   /dashboard   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /advanced-stats   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /ai-predictions   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /help-support   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /market-analysis   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /match-analysis   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /match-reports   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /match-schedule   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /matches   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /news   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /player-analytics   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /profile   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /settings   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /sports-coverage   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /team-performance   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /value-bets   [nav links: .html filenames -> SPA routes (intended)]
MATCH   /watchlist   [nav links: .html filenames -> SPA routes (intended)]

Result: 20/20 pages = UI match (all remaining notes are intended Next.js differences).
============================================================
UPDATE: CSS FOUC FIX VERIFIED (2026-09-02)
============================================================
Fix: All stylesheets are mounted once in the ROOT layout (src/app/styles-manager.js).
On each route the inactive section links get media="not all", so stylesheets never leave
document.head during SPA navigation -> no unstyled flash.

- SPA nav test: 10/10 transitions flash-free (dashboard<->dashboard, public<->public,
  dashboard->login, rapid double-nav).
- 20/20 page UI parity re-confirmed (DOM compare + screenshots).
- Pixel diff vs original HTML (1440x1000): 17/20 pages = 0.000%. team-performance 0.591%
  pre-exists (old build identical: team-performance-baseline-old-next.png).
- Language EN/FR: 12/12 checks passed (persists across SPA nav + full reloads, default FR).
